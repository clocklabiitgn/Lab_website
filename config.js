/* =========================================================================
   CLOCK Lab website — CONFIGURATION
   -------------------------------------------------------------------------
   This is the ONLY code file you may ever need to touch, and only once.

   Paste your Google Sheet ID between the quotes below, then save.
   (The ID is the long code in your sheet's web address, between
    "/d/" and "/edit" — see EDITING_GUIDE.md, Step 3.)

   Leave it empty ("") and the site simply shows its built-in content.
   ========================================================================= */
window.CLOCK_CONFIG = {
  sheetId: ""
};
